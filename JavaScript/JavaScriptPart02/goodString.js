// A good string is a string that starts with letter a and has a length greather than 3.

// let gString = "apple";
let gString = "bananan";

if(gString.length > 3 && gString[0] === "a"){
    console.log("Good string");
}